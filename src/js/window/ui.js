import Swiper from 'swiper';
import { Keyboard, Navigation, Pagination, } from 'swiper/modules';

window.__remontnick__ = {};

window.__remontnick__.swiper = {
  Swiper,
  modules: {
    Keyboard,
    Navigation,
    Pagination,
  }
}
